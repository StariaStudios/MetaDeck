{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">🛠 MetaDeck Admin Dashboard</h1>

        <div class="row">
            <!-- DeckTypes Panel -->
            <div class="col-md-6 mb-3">
                <div class="card border-primary h-100">
                    <div class="card-header bg-primary text-white">Deck Types</div>
                    <div class="card-body">
                        <p>Total DeckTypes: <strong>{{ deckTypes|length }}</strong></p>
                        <a href="{{ path('admin_deck_dashboard') }}" class="btn btn-primary btn-sm">View DeckTypes</a>
                        <a href="{{ path('admin_deck_new') }}" class="btn btn-success btn-sm">➕ Add New</a>
                    </div>
                </div>
            </div>

            <!-- Users Panel -->
            <div class="col-md-6 mb-3">
                <div class="card border-warning h-100">
                    <div class="card-header bg-warning text-dark">Users</div>
                    <div class="card-body">
                        <p>Total Users: <strong>{{ users|length }}</strong></p>
                        <a href="{{ path('admin_user_index') }}" class="btn btn-warning btn-sm">Manage Users</a>
                        <a href="{{ path('admin_user_new') }}" class="btn btn-success btn-sm">➕ Add User</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
